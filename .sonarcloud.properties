# SonarCloud automatic analysis configuration.
#
# Exclude machine-produced and vendored code from duplication detection:
# - lib/theme/themes/generated.ts is generated by scripts/import-tweakcn.mjs
#   (9 themes with an identical variable structure — inherently repetitive data).
# - components/ui/** and the lib files below are vendored byte-identical from
#   miskibin/chat-components (see AGENTS.md); they are not authored in this repo.
sonar.cpd.exclusions=lib/theme/themes/generated.ts,components/ui/**,hooks/use-click-outside.ts,lib/cursor-agent-types.ts,lib/cursor-stream.ts,lib/cursor-agent.ts,lib/agent-runtime.ts,lib/mock-agent.ts,lib/layout-transition.ts
